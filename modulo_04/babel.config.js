module.exports = {
  presets: [
    "@babel/preset-env", //Responsavel por traduzir as funcionalidades do JS que o browser não entende
    "@babel/preset-react" //Responsavel por traduzir as funcionalidades do react que o browser não entende
  ],
};